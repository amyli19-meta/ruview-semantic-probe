# ruview-semantic-inference

A small wellbeing-monitor library that turns a stream of fused RuView sensor
readings into HA-MIND semantic-automation primitives (ADR-115 §3.12).

The public API lives in `src/lib.rs`. The `SemanticEngine::push` FSMs are not
yet implemented — see the task instructions.
